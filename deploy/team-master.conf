server {
    listen 80;
    server_name your-domain.com;  # 修改为您的域名或服务器IP

    # 日志配置
    access_log /var/log/nginx/team-master-access.log;
    error_log /var/log/nginx/team-master-error.log;

    # 前端静态文件（如果有）
    # location / {
    #     root /var/www/team-master/frontend/dist;
    #     try_files $uri $uri/ /index.html;
    # }

    # API 反向代理
    location /api {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # 健康检查
    location /health {
        proxy_pass http://localhost:5000/health;
        access_log off;
    }
}

# HTTPS 配置（使用 Let's Encrypt 证书后启用）
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#
#     ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#
#     # ... 其他配置与上面相同
# }
